# Авторизация в Credit Scoring API

## Быстрый старт

### 1. Установка зависимостей

```bash
cd credit-score

# Создать виртуальное окружение
python3 -m venv venv

# Активировать окружение
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### 2. Запуск сервера

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Запустить сервер
uvicorn app.main:app --reload
```

### 3. Тестирование

```bash
# В новом терминале, активируйте окружение
source venv/bin/activate

# Запустите тестовый скрипт
python test_auth.py
```

## Учетные данные для тестирования

| Пользователь | Пароль   | Роль  | Доступ                          |
|--------------|----------|-------|---------------------------------|
| admin        | secret   | admin | Полный доступ ко всем эндпоинтам |
| user         | password | user  | Доступ к предсказаниям          |

## Защищенные эндпоинты

### Требуют авторизации (любой пользователь):
- `POST /predict` - Полное предсказание
- `POST /predict_slim` - Упрощенное предсказание
- `POST /predict/default` - Предсказание дефолта
- `POST /predict/score` - Предсказание скора
- `POST /predict/batch` - Пакетное предсказание

### Требуют роль администратора:
- `GET /model-info` - Информация о модели

### Публичные эндпоинты (без авторизации):
- `GET /` - Информация об API
- `GET /health` - Проверка здоровья
- `GET /features` - Список признаков
- `GET /statistics` - Статистика
- `POST /auth/login` - Авторизация
- `GET /auth/me` - Информация о текущем пользователе

## Использование

### 1. Получение токена

```bash
curl -X POST "http://localhost:8000/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "secret"}'
```

### 2. Использование токена

```bash
curl -X POST "http://localhost:8000/predict_slim" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"R_DEBT_INCOME": 1.78, "DEBT": 320000, "INCOME": 180000, "SAVINGS": 450000}'
```

## Swagger UI

Откройте http://localhost:8000/docs

1. Выполните запрос `/auth/login`
2. Скопируйте `access_token`
3. Нажмите кнопку "Authorize" (🔒)
4. Вставьте токен
5. Используйте защищенные эндпоинты

## Добавление новых пользователей

```bash
cd credit-score
python app/generate_password.py
```

Скопируйте сгенерированный хеш в `app/auth.py` в словарь `USERS_DB`.

## Безопасность

⚠️ **Для продакшена:**

1. Измените `SECRET_KEY` в `app/auth.py`
2. Используйте переменные окружения
3. Настройте HTTPS
4. Используйте реальную БД
5. Ограничьте CORS

## Структура файлов

```
credit-score/
├── app/
│   ├── auth.py              # Модуль авторизации
│   ├── main.py              # Основное приложение (обновлено)
│   └── generate_password.py # Генератор паролей
├── test_auth.py             # Тесты авторизации
├── AUTH_EXAMPLES.md         # Примеры использования
├── README_AUTH.md           # Эта инструкция
└── .env.example             # Пример конфигурации
```

## Примеры кода

Смотрите `AUTH_EXAMPLES.md` для примеров на Python, JavaScript и curl.
