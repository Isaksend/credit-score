# 🚀 Быстрый старт

## Автоматический запуск (самый простой способ)

```bash
cd credit-score
./start.sh
```

Скрипт автоматически:
- Создаст виртуальное окружение (если нужно)
- Установит все зависимости (если нужно)
- Запустит сервер

## Ручной запуск

```bash
cd credit-score

# 1. Создать виртуальное окружение
python3 -m venv venv

# 2. Активировать окружение
source venv/bin/activate

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Запустить сервер
uvicorn app.main:app --reload
```

## Проверка работы

Откройте в браузере: http://localhost:8000/docs

## Тестовые учетные данные

| Пользователь | Пароль   | Роль  |
|--------------|----------|-------|
| admin        | secret   | admin |
| user         | password | user  |

## Как использовать API

### 1. Получить токен

```bash
curl -X POST "http://localhost:8000/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "secret"}'
```

### 2. Сделать предсказание

```bash
curl -X POST "http://localhost:8000/predict_slim" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "R_DEBT_INCOME": 1.78,
    "DEBT": 320000,
    "INCOME": 180000,
    "SAVINGS": 450000
  }'
```

## Тестирование

```bash
# Активировать окружение
source venv/bin/activate

# Запустить тесты
python test_auth.py
```

## Документация

- 📖 **Swagger UI**: http://localhost:8000/docs
- 📘 **ReDoc**: http://localhost:8000/redoc
- 🔐 **Авторизация**: [AUTH_EXAMPLES.md](AUTH_EXAMPLES.md)
- 📦 **Установка**: [INSTALL.md](INSTALL.md)

## Остановка сервера

Нажмите `Ctrl+C` в терминале где запущен сервер
